# Copia este archivo como ".env" y ajusta la IP a la de tu máquina en la red local
# (no uses "localhost": el celular/emulador no es la misma máquina que el backend).
# En Windows puedes obtenerla con `ipconfig` (IPv4 Address).
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000/api
